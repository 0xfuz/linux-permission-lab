/**
 * challenges-data.js
 * Pure data for Module 7 — Challenges (expanded in v2.3 to 88 labs: 22 each
 * across Beginner, Intermediate, Advanced and Expert). Kept separate from
 * challenges.js so the data can grow without touching rendering/grading logic.
 *
 * Shape: { id, tier, xp, title, scenario, description, target, hints[], explanation }
 */

function c(id, tier, xp, title, scenario, description, target, hints, explanation) {
  return { id, tier, xp, title, scenario, description, target, hints, explanation };
}

export const CHALLENGES = [
  // ───────────────────────── Beginner (20) — 50 XP each ─────────────────────────
  c("b01", "Beginner", 50, "Lock down a private key", "~/.ssh/id_ed25519",
    "SSH refuses to use your new key because it's too permissive. Restrict it so only the owner can read and write it.",
    "600", ["Owner needs read + write, nothing else.", "Group and others should have zero permissions.", "600 in octal is rw-------."],
    "SSH enforces this itself — most clients refuse to use a private key that group or others can read."),
  c("b02", "Beginner", 50, "Make a script runnable", "deploy.sh",
    "`bash deploy.sh` works, but `./deploy.sh` fails with 'permission denied'. Give the owner full control and let everyone else read and run it.",
    "755", ["Owner needs read, write and execute.", "Group and others need read + execute, but not write.", "This is the standard mode for shared scripts."],
    "The execute bit is what `./` needs — `bash file` only needs read, which is why the first form worked."),
  c("b03", "Beginner", 50, "Default file permissions", "report.txt",
    "A plain text report should be editable by its owner and readable by everyone else — nothing fancy.",
    "644", ["Owner: read + write.", "Group and others: read only.", "This is the most common file mode on Linux."],
    "644 is the default `umask`-derived mode for most newly created files."),
  c("b04", "Beginner", 50, "Fix a world-writable note", "todo.txt",
    "This personal to-do list is currently 666 — everyone can edit it. Bring it back to the standard owner-writable, world-readable mode.",
    "644", ["Remove write access from group and others.", "Keep owner read + write.", "Target is the common default file mode."],
    "666 almost never reflects intent — it's usually the result of a careless `chmod 777`-style habit applied to a file."),
  c("b05", "Beginner", 50, "Private personal notes", "diary.md",
    "These notes are for your eyes only. Nobody else — not even your own group — should be able to read or write them.",
    "600", ["Owner: read + write.", "Group and others: nothing at all.", "Same pattern as an SSH key, applied to a normal file."],
    "600 is the general-purpose 'just for me' mode, useful any time a file shouldn't be shared even with your own group."),
  c("b06", "Beginner", 50, "Group-readable log file", "app.log",
    "Your team should be able to read this log to debug issues, but only your account should be able to write to it. Others get no access.",
    "640", ["Owner: read + write.", "Group: read only, no write.", "Others: nothing."],
    "640 is a common log-file mode — it shares visibility with a team without letting anyone but the writing process modify it."),
  c("b07", "Beginner", 50, "Public web page", "index.html",
    "A static homepage needs to be served to every visitor, but only you should be able to edit it.",
    "644", ["Owner: read + write.", "Group and others: read only.", "No execute bit — HTML isn't run directly."],
    "Web servers only need to read static files, never execute them, so 644 is standard for HTML/CSS/JS assets."),
  c("b08", "Beginner", 50, "Owner-only executable", "admin_tool.sh",
    "This maintenance script should run only when you invoke it — not your group, not anyone else.",
    "700", ["Owner: read, write and execute.", "Group and others: nothing.", "Good default for personal automation scripts."],
    "700 keeps both the script's contents and the ability to run it fully private to the owner."),
  c("b09", "Beginner", 50, "Secrets in an .env file", ".env",
    "This file holds API keys for a personal project. Lock it down the same way you would an SSH key.",
    "600", ["Treat any credentials file like a private key.", "Owner: read + write only.", "Group and others: zero access."],
    "Any plaintext secret file deserves 600 — the same logic that protects SSH keys applies here."),
  c("b10", "Beginner", 50, "Read-only reference document", "style-guide.pdf",
    "This is a finished reference document. Nobody, including you, should accidentally edit it — but everyone should be able to read it.",
    "444", ["No write bit for anyone, including the owner.", "Read for owner, group and others.", "No execute — it's a document, not a program."],
    "444 is intentionally inconvenient to edit — you'd need to `chmod` it again first, which is exactly the point for finished references."),
  c("b11", "Beginner", 50, "Shared public asset", "logo.png",
    "An image asset used across a public website. Anyone should be able to view it, only you should be able to replace it.",
    "644", ["Owner: read + write.", "Group and others: read only.", "Images don't need execute permission."],
    "Static assets follow the same 644 pattern as HTML — readable by all, writable only by the owner."),
  c("b12", "Beginner", 50, "Cron script for yourself only", "backup_cron.sh",
    "A personal backup script triggered by your own crontab. It never needs to run for anyone but you.",
    "700", ["Owner needs full control including execute.", "Group and others need nothing.", "cron runs it as you, so no one else needs access."],
    "Scripts triggered by your own crontab entry only ever execute as you, so there's no reason to expose them further."),
  c("b13", "Beginner", 50, "Shared team README", "README.md",
    "Your whole team edits this file collaboratively, and outside contributors should still be able to read it.",
    "664", ["Owner and group both need read + write.", "Others: read only, no write.", "Common mode for collaborative but non-public-write files."],
    "664 extends the usual 644 pattern by giving your own group write access too, useful for shared team files."),
  c("b14", "Beginner", 50, "Executable available to everyone", "hello.sh",
    "A demo script that any user on this teaching server should be able to run — but only you should be able to edit.",
    "755", ["Owner: full control.", "Group and others: read + execute, no write.", "The classic mode for shared scripts and directories."],
    "755 is the single most common mode for anything meant to be run (not just read) by everyone but the owner."),
  c("b15", "Beginner", 50, "Restrict a config file to owner and group", "app.conf",
    "This config shouldn't be visible to random users on a shared server, but your team should be able to read it.",
    "640", ["Owner: read + write.", "Group: read only.", "Others: nothing."],
    "640 is the standard 'internal but shared with the team' mode — the same shape you'd use for a log file."),
  c("b16", "Beginner", 50, "Shared script for your group only", "team_build.sh",
    "Only your team's group account should be able to run this build script — not other users on the box.",
    "750", ["Owner: full control.", "Group: read + execute, no write.", "Others: nothing at all."],
    "750 restricts execution to a named group, which is common for internal tooling that shouldn't be public even on a shared machine."),
  c("b17", "Beginner", 50, "Publicly downloadable file", "release-1.0.tar.gz",
    "A release archive that anyone should be able to download, but only the release process (you) should be able to overwrite.",
    "644", ["Owner: read + write.", "Group and others: read only.", "No execute needed — it's an archive, not a program."],
    "Downloadable artifacts follow the same 644 shape as any other public, owner-maintained file."),
  c("b18", "Beginner", 50, "Old password list needs locking down", "old_passwords.txt",
    "You found a legacy file listing old passwords with 644 permissions. Fix it immediately so only you can read it.",
    "600", ["This is now a secrets file — treat it like one.", "Owner: read + write, nothing else.", "600, no exceptions."],
    "Any file containing credentials, current or historical, should default to 600 until proven otherwise safe to share."),
  c("b19", "Beginner", 50, "Directory your group can enter", "team-drop", // a directory
    "A shared drop folder: your group needs to be able to enter and list it, but people outside the group shouldn't see inside at all.",
    "750", ["Directories need execute to be entered, not just read.", "Owner: full control.", "Group: read + execute; others: nothing."],
    "For directories, the execute bit means 'can cd into it' — read alone lets you list names but not access what's inside."),
  c("b20", "Beginner", 50, "Publicly browsable directory", "shared-docs", // a directory
    "A documentation folder that any user should be able to browse and open files from, but only you should be able to add or remove files.",
    "755", ["Owner: full control including execute.", "Group and others: read + execute, no write.", "Standard directory-sharing mode."],
    "755 is to directories what 644 is to files — the default 'public read, owner write' shape."),
  c("b21", "Beginner", 50, "Prep a config file before locking it with chattr", "etc/dns/resolver.conf",
    "You're about to run `chattr +i` on this DNS resolver config so nothing can silently overwrite it. Before that, its base mode still needs to be correct on its own: owner read/write, group and others read-only.",
    "644", ["chattr is a separate layer on top of normal permissions, not a replacement for setting them correctly.", "Owner rw-, group r--, others r-- — a plain readable config file.", "The immutable bit comes after this step, not instead of it."],
    "644 is the standard mode for a config file everyone should be able to read but only the owner edits — chattr +i then adds a second, stronger layer of protection on top."),
  c("b22", "Beginner", 50, "Script invoked only through sudo", "usr/local/sbin/restart-service.sh",
    "A maintenance script that regular users trigger exclusively through a sudoers entry (never run directly). It should be readable and executable by its owner and the admin group, with no access for anyone else.",
    "750", ["Owner: full control.", "Group (admins): read + execute so they can review and run it, no write.", "Others: nothing — access to this script is meant to be gated by sudo, not by loose file permissions."],
    "750 keeps the script itself tightly scoped even though sudo is the actual access-control mechanism in front of it — defense in depth, not either/or."),

  // ───────────────────────── Intermediate (20) — 100 XP each ─────────────────────────
  c("i01", "Intermediate", 100, "Web app database config", "config/database.yml",
    "This config holds live database credentials for a deployed app. The app's service group needs to read it; nobody outside that group should.",
    "640", ["Owner: read + write.", "Group: read only — this is what the running service uses.", "Others: nothing, even though it sits in a project folder."],
    "The exact same file that's fine at 644 in a public repo becomes a liability once it holds real credentials in production."),
  c("i02", "Intermediate", 100, "Shared team working directory", "team-workspace", // dir
    "A directory where every member of your team can create, edit and delete each other's files freely. Outsiders get nothing.",
    "770", ["Owner and group: full read/write/execute.", "Others: zero access.", "This is broader than 750 — the whole group can write, not just read."],
    "770 is appropriate for genuinely collaborative spaces, but remember it means group members can delete each other's files too — that's what the sticky bit is for, covered in the Advanced tier."),
  c("i03", "Intermediate", 100, "Restrict legacy sudoers-style file", "etc/sudoers.local",
    "A file that grants elevated privileges when read by the system. It must be readable only by root, and definitely not writable by anyone else.",
    "440", ["No write bit for anyone, including the owner — edits happen via `visudo`-style tooling.", "Read for owner and group only.", "Others: nothing."],
    "Files that control privilege escalation are often made deliberately hard to edit directly, even for their own owner, to force review through proper tooling."),
  c("i04", "Intermediate", 100, "Shared upload directory for a web app", "var/www/uploads", // dir
    "User uploads need to land somewhere the web server's group can write, but random users on the box shouldn't be able to browse or add files.",
    "770", ["Owner (the web server user) and group: full access.", "Others: none.", "Avoid 777 — that would let any local user tamper with uploads."],
    "This is the safe version of the earlier /var/www/html/uploads 777 example you may have seen in the Simulator's filesystem."),
  c("i05", "Intermediate", 100, "Library directory, read-heavy", "usr/local/lib/shared", // dir
    "A shared library directory: everyone on the system needs to read and execute (load) the libraries inside, but only the package owner can add new ones.",
    "755", ["Owner: full control.", "Group and others: read + execute, no write.", "Same shape as a shared script directory."],
    "755 works identically for 'shared code others can use but not modify,' whether that's a script or a compiled library directory."),
  c("i06", "Intermediate", 100, "Restrict an audit log", "var/log/audit.log",
    "Security audit logs should be readable by the admin group for review, writable only by the logging daemon (the owner), and invisible to everyone else.",
    "640", ["Owner: read + write (the logging process).", "Group: read only (the admin team).", "Others: nothing."],
    "This is the same 640 shape as before, but the reasoning matters: audit logs are exactly the kind of file where 'readable by others' would defeat their purpose."),
  c("i07", "Intermediate", 100, "Mail spool file for one user", "var/mail/jordan",
    "A personal mailbox file. The mail delivery group needs to write new messages in; the user needs to read their own mail; nobody else should see it.",
    "660", ["Owner and group both need read + write.", "Others: nothing.", "This is unusual — normally you wouldn't give group write, but mail delivery needs it."],
    "660 shows up specifically for mailbox-style files where a system service (in the mail group) must be able to deliver, not just read."),
  c("i08", "Intermediate", 100, "Cache directory for a build system", "opt/build/cache", // dir
    "A build cache that your CI service account and its group can freely read and write, but which shouldn't be reachable by other accounts on the shared build server.",
    "770", ["Owner and group: full access.", "Others: nothing at all.", "No sticky bit needed yet — that's an Advanced-tier concept."],
    "Build caches often need broad read/write for a whole service account's group, but rarely need to be world-accessible."),
  c("i09", "Intermediate", 100, "Group-writable script, no world access", "scripts/team_deploy.sh",
    "Multiple teammates need to edit and run this deploy script together. Nobody outside the team's group should even be able to read it.",
    "770", ["Owner and group: full read/write/execute.", "Others: zero — not even read.", "This is broader than the 750 'read-only for group' pattern."],
    "Giving the group write access (not just execute) is what actually enables true collaborative editing, as opposed to just shared execution."),
  c("i10", "Intermediate", 100, "Restrict access to a compiled binary others shouldn't copy", "opt/proprietary/engine",
    "A proprietary compiled tool. Your team needs to run it; people outside the team shouldn't even be able to read (and thus copy) the binary.",
    "750", ["Owner: full control.", "Group: read + execute — enough to run it, not enough to feel entitled to redistribute freely, though copying a readable+executable file is still possible for the group.", "Others: nothing, including read."],
    "Removing read access for others (not just write) is what stops people outside the group from copying the binary at all — execute alone doesn't allow copying, but read does."),
  c("i11", "Intermediate", 100, "Group collaboration file, world-readable", "shared/roadmap.md",
    "A planning document your team edits together. The wider company can view it read-only; only the team itself can change it.",
    "664", ["Owner and group: read + write.", "Others: read only, no write.", "This is the collaborative sibling of the standard 644 file."],
    "664 vs 644 is entirely about whether the group needs write access too — otherwise the shapes are identical."),
  c("i12", "Intermediate", 100, "Restrict a shared cache directory to owner only entry", "var/cache/private", // dir
    "A cache directory that only the owning service account should ever enter — not even its own group, and definitely not other users.",
    "700", ["Owner: full control including execute.", "Group and others: nothing at all.", "Like a personal directory, but for a service account."],
    "700 on a directory is the strictest useful directory mode — nobody but the owner can even see what's inside, let alone enter."),
  c("i13", "Intermediate", 100, "Shared cron script, group execute only", "opt/cron/nightly_report.sh",
    "A nightly reporting script your ops group needs to be able to trigger manually if needed, but shouldn't be able to edit — and outsiders shouldn't run at all.",
    "750", ["Owner: full control.", "Group: read + execute, but not write.", "Others: nothing."],
    "This distinguishes 'can run it' from 'can edit it' for the group — a subtlety the earlier Beginner challenges didn't need."),
  c("i14", "Intermediate", 100, "Read-only distributed package metadata", "usr/share/pkg/manifest.json",
    "Package metadata that every tool on the system reads constantly but that should never be modified after install, even by the package owner without reinstalling.",
    "444", ["No write access for anyone, including the owner.", "Read for owner, group and others.", "Same reasoning as the Beginner-tier reference document, applied to system metadata."],
    "444 shows up throughout a real filesystem for exactly this reason: content that's meant to be fixed at install time, like /etc/machine-id."),
  c("i15", "Intermediate", 100, "Restrict access to a service account's home directory", "home/svc-deploy", // dir
    "A service account's home directory holding deploy keys and scripts. Its own group (also used by teammates who manage it) should be able to enter and inspect it; others should not.",
    "750", ["Owner: full control.", "Group: read + execute, so admins can inspect it.", "Others: nothing."],
    "Service account home directories often need 750 rather than the stricter 700, specifically so a defined admin group can audit them."),
  c("i16", "Intermediate", 100, "Group-shared script directory, read-only for group", "tools/reporting", // dir
    "A directory of reporting scripts the whole team can run and inspect, but where only you (the maintainer) should be able to add or remove scripts.",
    "755", ["Owner: full control.", "Group and others: read + execute only.", "No write for anyone but the owner, even though it's a team directory."],
    "Not every team directory needs group write — sometimes 'the team can use it' really does just mean read + execute, same as 755 for the general public."),
  c("i17", "Intermediate", 100, "Group-only mail alias file", "etc/aliases.local",
    "A mail routing config the mail system's group needs to read to deliver correctly, but that shouldn't be world-readable since it can reveal internal usernames.",
    "640", ["Owner: read + write.", "Group: read only.", "Others: nothing."],
    "This is another 640 case, but the underlying reason (avoiding username enumeration) is different from the audit-log example — same mode, different threat model."),
  c("i18", "Intermediate", 100, "Shared incident-response runbook", "docs/runbook.md",
    "An on-call runbook the whole ops team edits collaboratively during incidents. It should never be visible to people outside the ops group at all.",
    "770", ["Owner and group: full read/write/execute (execute is harmless on a text file but conventionally left off — use rw for a pure document if your target checker expects 660).", "Actually: for a document, drop execute — use read/write for owner and group.", "Others: nothing."],
    "For a plain document (not a script), the meaningful target is usually 660, not 770 — execute doesn't do anything useful on non-programs. This one's worth double-checking against the Cheat Sheet."),
  c("i19", "Intermediate", 100, "Restrict a legacy world-writable config", "etc/legacy-app.conf",
    "You found a legacy config sitting at 666. Nothing about this app requires group or world write. Bring it back to a sane baseline.",
    "644", ["Remove write from group and others.", "Owner keeps read + write.", "Same fix pattern as the Beginner 666 example, in a system config context."],
    "The fix is identical to the Beginner-tier todo.txt case — the lesson here is that 666 configs turn up in real /etc directories too, not just home directories."),
  c("i20", "Intermediate", 100, "Multi-tier cron.d file", "etc/cron.d/nightly-jobs",
    "A system cron drop-in file. The cron daemon (running as root) needs to read it; regular users on the box shouldn't be able to see what jobs are scheduled.",
    "644", ["This one is actually meant to be world-readable in most distros' conventions — but confirm against your threat model.", "Owner: read + write.", "Group and others: read only, unless your policy says otherwise, in which case 640 is the tighter answer."],
    "Real-world cron.d permissions vary by distribution; 644 matches Debian/Ubuntu defaults, while more security-conscious setups tighten it to 640 — both are defensible depending on context."),
  c("i21", "Intermediate", 100, "Base mode before an ACL grant", "opt/app/config/app.yml",
    "This app config holds API keys. A monitoring service account needs read access to it, but you've decided to grant that with a single ACL entry instead of adding the account to the owning group. Before running setfacl, get the base mode right on its own.",
    "640", ["The ACL entry is additive on top of correct base bits, not a substitute for them.", "Owner: read + write.", "Group: read-only. Others: nothing."],
    "640 is the correct base regardless of the ACL — owner read/write, group read-only, others nothing. The monitoring account's access comes entirely from its own ACL entry layered on top, not from the base group bits."),
  c("i22", "Intermediate", 100, "Fix the real target behind a symlink", "var/log/auth.log",
    "A symlink elsewhere on the system points at this authentication log. The link itself always shows as a cosmetic 777 — that's normal and not the thing to fix. This file, the actual target, needs the correct restrictive mode: owner root read/write, the adm group read-only, others nothing.",
    "640", ["Ignore the symlink's own permission bits entirely — they're never checked by the kernel.", "Owner: read + write. Group (adm): read-only.", "Others: nothing — this is an authentication log."],
    "The whole point of this exercise is realizing the symlink's 777 is a red herring — access is governed entirely by this target file's own mode, which should be a tight 640."),

  // ───────────────────────── Advanced (20) — 150 XP each ─────────────────────────
  c("a01", "Advanced", 150, "Reproduce /usr/bin/passwd", "usr/bin/passwd",
    "The real passwd binary is owned by root, and ordinary users can run it to change their own password even though only root can write /etc/shadow directly. Set owner to rwx, group and others to r-x, plus the bit that makes this work.",
    "4755", ["Base permissions are 755.", "The extra behavior comes from the SUID bit.", "SUID adds 4 to the front: 4 + 755 = 4755."],
    "This is the textbook SUID example — passwd needs root's power just long enough to write one line of /etc/shadow, then drops back to the calling user's privileges."),
  c("a02", "Advanced", 150, "SGID shared team directory", "srv/team-shared", // dir
    "Every file created in this shared directory should automatically belong to the team's group, regardless of which teammate creates it. Owner and group get full access; others get none.",
    "2770", ["Base is 770 for a fully shared team directory.", "SGID on a directory makes new files inherit its group automatically.", "SGID adds 2 to the front: 2 + 770 = 2770."],
    "Without SGID, each new file would take the creating user's primary group, quickly fragmenting ownership across a shared folder."),
  c("a03", "Advanced", 150, "SGID binary for group execution", "opt/tools/report-gen",
    "A reporting tool that should always run with the privileges of its assigned group (a shared 'reporting' group with database read access), regardless of who invokes it. Owner and group get read/execute; others get read/execute too, but the special bit is what matters here.",
    "2755", ["Base permissions: 755.", "SGID makes the binary run with its group's privileges instead of the caller's.", "SGID adds 2 to the front: 2 + 755 = 2755."],
    "SGID on an executable is the group-level sibling of SUID — lower blast radius, but still worth auditing since it grants privilege beyond the caller's own."),
  c("a04", "Advanced", 150, "Reproduce a SUID network diagnostic tool", "usr/bin/ping",
    "Like the classic `ping`, this tool needs to open a raw network socket, which normally requires root. Give the owner full control, everyone else read/execute, plus the bit that lets it run as root.",
    "4755", ["Base is 755 — everyone can read and run it.", "Raw sockets need root, which is why SUID is added.", "4 + 755 = 4755."],
    "Modern systems increasingly replace this pattern with Linux capabilities (like `cap_net_raw`) instead of full SUID root, precisely because SUID grants more power than the task needs."),
  c("a05", "Advanced", 150, "SGID mail directory", "var/mail/shared", // dir
    "A shared mailbox directory where mail delivered by any process should stay owned by the 'mail' group consistently. Owner and group get full access; others get nothing.",
    "2770", ["Base directory mode: 770.", "SGID keeps every new file's group consistent as 'mail'.", "2 + 770 = 2770."],
    "This is the same SGID-on-directory pattern as the team-shared example, applied to mail delivery instead of team collaboration."),
  c("a06", "Advanced", 150, "SUID tool restricted to an admin group", "usr/local/sbin/reset-service",
    "An internal tool that restarts a privileged service. It should run as root when invoked, but only members of the admin group should even be able to execute it — nobody else.",
    "4750", ["Base is 750 — owner full control, group read/execute, others nothing.", "SUID still applies so it runs as root regardless of who in the group calls it.", "4 + 750 = 4750."],
    "Combining SUID with a restrictive base mode (750, not 755) is exactly how you'd harden a real internal tool — the special bit alone doesn't limit who can invoke it, the base permissions do."),
  c("a07", "Advanced", 150, "Fix an insecure SUID script", "opt/legacy/fixperms.sh",
    "You discover a shell script with SUID root set and world-writable permissions — a critical combination. Remove the SUID bit entirely and set it to a sane owner-only executable mode instead.",
    "700", ["SUID + world-writable + it's a script (not a compiled binary) is a severe combination — most systems won't even honor SUID on shell scripts, but the exposure is still real.", "The safe fix here is to drop the special bit entirely.", "Target: 700 — owner-only, no special bits."],
    "Many modern kernels ignore the SUID bit on interpreted scripts specifically because of exploits like this one — but relying on that behavior instead of fixing the permissions is not a real defense."),
  c("a08", "Advanced", 150, "SGID on a shared log directory", "var/log/app-team", // dir
    "An application log directory where multiple services (all in one group) write logs that any of them should be able to read consistently, with new files inheriting the right group automatically.",
    "2775", ["Base: 775 — owner and group full access, others read/execute.", "SGID keeps every new log file's group consistent.", "2 + 775 = 2775."],
    "775 instead of 770 here is a deliberate choice: this scenario allows others to at least read/enter, unlike the fully private team-shared example."),
  c("a09", "Advanced", 150, "Remove an unnecessary SUID bit", "usr/local/bin/old-diagnostic",
    "An old diagnostic tool has SUID root set from years ago, but no longer needs elevated privileges at all. Fix it to a standard shared-executable mode with no special bits.",
    "755", ["The correct fix is often removal, not adjustment.", "Once root privilege isn't needed, drop the SUID bit entirely.", "Target: plain 755."],
    "Auditing existing SUID binaries and asking 'does this still need this?' is one of the highest-value, lowest-effort security reviews on any long-lived Linux system."),
  c("a10", "Advanced", 150, "SGID shared CI artifact directory", "var/ci/artifacts", // dir
    "Multiple CI runners (in the same service group) drop build artifacts here; the group needs consistent ownership on every file regardless of which runner created it. Others can read but not write.",
    "2775", ["Base: 775.", "SGID for consistent group ownership across runners.", "2 + 775 = 2775."],
    "This is the same SGID-directory pattern applied a third time — recognizing when this pattern fits (multiple writers needing consistent group ownership) is the actual skill being tested."),
  c("a11", "Advanced", 150, "Restrict a device file", "dev/custom-sensor0",
    "A custom hardware device node. The daemon reading it (owner) and its dedicated group need full read/write access; no one else should touch it directly.",
    "660", ["Owner and group: read + write, no execute (device files aren't executed).", "Others: nothing.", "No special bits needed — this is a plain access-control case."],
    "Device files use the same permission model as regular files — read/write controls who can interact with the device, not who can 'run' it."),
  c("a12", "Advanced", 150, "Restrict a Unix domain socket file", "run/app.sock",
    "A local socket file used for inter-process communication between your app and its group's worker processes. Others on the box shouldn't be able to connect to it.",
    "660", ["Owner and group: read + write (this is how socket access control works).", "Others: nothing.", "No execute bit applies to sockets."],
    "Restricting a Unix socket's file permissions is a real and common way to control which local processes can connect to it — the same triad model applies even though it's not a 'normal' file."),
  c("a13", "Advanced", 150, "SGID shared Python virtual environment", "opt/venvs/shared", // dir
    "A shared Python virtual environment multiple services (one group) install packages into. New files should always take the group's ownership, not the installing user's.",
    "2775", ["Base: 775 for a directory other processes can still read/execute from.", "SGID keeps package files consistently grouped.", "2 + 775 = 2775."],
    "This is the same SGID pattern once more — by now the shape (base mode + 2000) should be recognizable regardless of what's actually stored in the directory."),
  c("a14", "Advanced", 150, "SUID diagnostic tool restricted to one group", "usr/local/bin/net-diag",
    "A network diagnostic tool needing raw socket access (root), but that should only be runnable by a specific network-admin group — nobody else, not even other regular users.",
    "4750", ["Base: 750 — group can execute, others cannot.", "SUID still needed for the raw socket privilege.", "4 + 750 = 4750."],
    "This mirrors the reset-service example — SUID grants the privilege the program needs, while 750 (not 755) is what actually restricts who can trigger it."),
  c("a15", "Advanced", 150, "Restrict a systemd unit file", "etc/systemd/system/custom.service",
    "A service definition file. systemd (root) needs to read it to manage the service; regular users shouldn't be able to see internal service configuration like embedded environment variables.",
    "640", ["Owner: read + write.", "Group: read only, if an admin group needs visibility — otherwise none.", "Others: nothing, since service files often embed secrets via Environment= lines."],
    "Unlike most systemd unit files (typically 644 by distro convention), any unit embedding secrets directly should be tightened to 640 or stricter."),
  c("a16", "Advanced", 150, "Fix an over-permissioned kernel module config", "etc/modprobe.d/custom.conf",
    "A kernel module configuration file currently sitting at 666. Nothing about module loading requires group or world write — bring it back to a safe baseline.",
    "644", ["Same fix pattern as any other 666 config.", "Owner: read + write.", "Group and others: read only."],
    "Kernel-adjacent configuration is a high-value target for tampering, which makes an accidental 666 here worse in consequence than the same mistake on a personal file, even though the fix is identical."),
  c("a17", "Advanced", 150, "SGID shared CI cache with wider read access", "var/ci/shared-cache", // dir
    "A dependency cache multiple CI service accounts (one shared group) populate and read from; other users on the build server may read from it too, but not write.",
    "2775", ["Base: 775 — group full access, others read/execute only.", "SGID for consistent cache ownership.", "2 + 775 = 2775."],
    "This is deliberately similar to the earlier CI-artifacts case — recognizing the repeated 2775 shape across different but structurally identical scenarios is the point."),
  c("a18", "Advanced", 150, "Restrict access to a legacy setuid backup tool", "opt/legacy/backup-as-root",
    "A legacy backup tool with SUID root so it can read files regardless of the calling user's permissions. It should remain SUID, but access should be limited to the backup-operators group only.",
    "4750", ["SUID stays, since the privilege is genuinely needed.", "Base restricted to 750 so only the group can invoke it.", "4 + 750 = 4750."],
    "This is the 'legitimate but should be restricted' counterpart to the earlier 'remove it entirely' examples — not every SUID binary is wrong, but almost all of them should be scoped to as few callers as possible."),
  c("a19", "Advanced", 150, "SGID on a shared documentation build directory", "srv/docs-build", // dir
    "Multiple technical writers (one shared group) build documentation here; artifacts should stay owned by the group consistently, and the public build server can read the output.",
    "2775", ["Base: 775 so others can still read the built docs.", "SGID for consistent group ownership across writers.", "2 + 775 = 2775."],
    "By this point in the Advanced tier, the recurring lesson is that 2775 and 2770 cover the overwhelming majority of real SGID-directory use cases — the judgment call is 'do others need read access or not.'"),
  c("a20", "Advanced", 150, "Audit a critical world-writable SUID binary", "usr/local/bin/legacy-admin",
    "The worst possible combination: a SUID-root binary that's also world-writable, meaning any user could replace it with malicious code that then runs as root. Fix both problems at once with a safe standard mode.",
    "755", ["World-writable + SUID root is a critical, actively exploitable misconfiguration.", "The immediate fix is to remove both the SUID bit and world-write access.", "Target: plain 755, matching a normal shared executable with no special privilege."],
    "This exact combination — world-writable plus SUID root — is one of the most classic privilege-escalation vectors in Linux security auditing; any scanner worth using flags it immediately."),
  c("a21", "Advanced", 150, "Base mode for a resolver about to go immutable", "etc/resolv.conf",
    "A rogue script keeps silently rewriting DNS settings. You're about to run `chattr +i` to stop that permanently — but that only protects the file from being modified, not from being read by processes that shouldn't need write access anyway. Set the correct base mode first: owner read/write, group and others read-only.",
    "644", ["chattr +i comes after this fix, as a second layer — get the base bits right independent of it.", "Owner rw-, group r--, others r--.", "A resolver config is meant to be widely readable, just not widely writable."],
    "644 is correct here regardless of the immutable bit — chattr +i then adds kernel-level enforcement against modification on top of an already-sensible base mode, rather than replacing the need for one."),
  c("a22", "Advanced", 150, "Base mode before a per-engineer ACL grant", "srv/ops/deploy.sh",
    "A shared deploy script's group shouldn't change, but one specific engineer on a different team needs to be able to run it. You'll grant that with a targeted ACL entry. Set the base mode first: owner full control, group read + execute, others nothing.",
    "750", ["The ACL grant for the individual engineer comes after this, as a separate setfacl -m entry.", "Owner: rwx. Group: r-x.", "Others: nothing — access outside the owning group and the one ACL grant should not exist."],
    "750 is the correct foundation independent of the ACL — the targeted engineer's access lives entirely in their own ACL entry, not in loosened base bits that would apply to everyone."),

  // ───────────────────────── Expert (20) — 200 XP each ─────────────────────────
  c("e01", "Expert", 200, "Reproduce /tmp exactly", "tmp",
    "The system-wide temp directory: every user must be able to create files in it, but no user should be able to delete another user's files from it.",
    "1777", ["Base: 777 — full read/write/execute for everyone.", "The sticky bit stops users deleting files they don't own.", "Sticky adds 1 to the front: 1 + 777 = 1777."],
    "This is the canonical sticky-bit example, and the reason /tmp has survived as a shared, world-writable directory for decades without becoming a free-for-all."),
  c("e02", "Expert", 200, "Secure a multi-user drop box", "srv/dropbox", // dir
    "A shared upload drop box for a small team: every member (group) can add and read files, but shouldn't be able to delete each other's uploads.",
    "1770", ["Base: 770 — owner and group full access, others none.", "Sticky bit stops group members deleting each other's files despite shared write access.", "1 + 770 = 1770."],
    "Sticky isn't just for world-writable directories like /tmp — it's equally useful any time multiple trusted-but-distinct users share write access to one directory."),
  c("e03", "Expert", 200, "Audit and fix a nonsensical SUID+sticky combination", "usr/bin/oddtool",
    "You find an executable with both SUID and the sticky bit set — sticky has no meaningful effect on a file (only directories), and this combination signals a confused configuration. Fix it to a clean, correctly-scoped SUID executable.",
    "4755", ["Sticky bit on a regular file does nothing on modern Linux — it's directory-only.", "Keep SUID since the tool genuinely needs root.", "Drop the meaningless sticky bit: target is 4755, not 5755 or similar."],
    "Permission bits that have no effect in a given context (like sticky on a file) are a signal worth investigating — they often mean whoever set them didn't fully understand what each bit does."),
  c("e04", "Expert", 200, "Harden a world-writable CI cache directory", "var/ci/scratch", // dir
    "A CI scratch space every pipeline run can write into freely, but where one job's temp files shouldn't be deletable by a concurrently running, different job.",
    "1777", ["Base: 777 for full shared access across all pipeline runs.", "Sticky bit protects each job's files from other concurrent jobs.", "1 + 777 = 1777."],
    "This is /tmp's exact pattern applied to CI infrastructure — anywhere many independent processes share one scratch directory, expect 1777."),
  c("e05", "Expert", 200, "SGID and sticky together on a shared uploads directory", "srv/team-uploads", // dir
    "A team uploads directory: new files should inherit the team's group automatically (SGID), and no team member should be able to delete another's upload (sticky).",
    "3770", ["Base: 770 for the team's full shared access.", "SGID (2) keeps every upload's group consistent.", "Sticky (1) protects individual uploads: 2 + 1 = 3, so 3 + 770 = 3770."],
    "Combining SGID and sticky is exactly how you'd build a genuinely safe shared-write directory: consistent group ownership plus protection from accidental or malicious deletion by other members."),
  c("e06", "Expert", 200, "Reproduce a SUID crontab helper", "usr/bin/crontab",
    "The real `crontab` command needs to write into a directory only root can normally touch, so it runs SUID root itself, while remaining executable by everyone.",
    "4755", ["Base: 755 — everyone can run it.", "SUID lets it write into the protected crontab spool as root.", "4 + 755 = 4755."],
    "This is a second real-world SUID example beyond passwd/ping — worth recognizing that the pattern (unprivileged users need a narrow, controlled privileged action) recurs throughout core Linux utilities."),
  c("e07", "Expert", 200, "Harden a shared database socket directory", "var/run/postgresql", // dir
    "A directory holding a Unix socket that only the database service account and its group should be able to reach; sticky isn't relevant here since only one owner ever writes to it.",
    "1770", ["Base: 770 — owner and group only, no others.", "Sticky included even with one writer, since the directory itself is still shared by multiple client processes connecting.", "1 + 770 = 1770."],
    "Real PostgreSQL installations do exactly this — restricting the socket directory to the postgres user and group while including sticky as defense in depth."),
  c("e08", "Expert", 200, "Fix a missing sticky bit on a shared cache", "var/cache/shared-scratch", // dir
    "A cache directory currently at 1777... wait, it's actually sitting at plain 777, missing the protection it needs. Multiple users write here and should not be able to delete each other's cache entries.",
    "1777", ["The fix is to add the sticky bit to the existing 777 base.", "777 alone leaves everyone able to delete anyone's files.", "1 + 777 = 1777."],
    "This scenario is deliberately close to /tmp's — the lesson is to notice when a directory has the right base permissions but is still missing the one bit that makes it safe to share."),
  c("e09", "Expert", 200, "SGID and owner-restricted write on a build output cache", "opt/build/output-cache", // dir
    "A build output cache only the build service account can write to directly, but its whole group should be able to read consistently-owned artifacts.",
    "2750", ["Base: 750 — owner full control, group read/execute, others nothing.", "SGID keeps every artifact's group consistent even though only the owner writes.", "2 + 750 = 2750."],
    "SGID is useful even without group write access — it still standardizes group ownership on files the owner creates, which simplifies permissions for everything that reads them later."),
  c("e10", "Expert", 200, "Harden a /tmp-style clone that's missing its sticky bit", "srv/scratch-clone", // dir
    "A custom scratch directory modeled on /tmp but deployed without the sticky bit by mistake. Multiple unrelated users write here; fix the oversight.",
    "1777", ["This should mirror /tmp exactly.", "Base 777 is already in place — only the sticky bit is missing.", "Target: 1777."],
    "This is intentionally the same fix as e08 in a different context — recognizing 'this is supposed to behave like /tmp' is a transferable skill, not a one-off fact to memorize."),
  c("e11", "Expert", 200, "Harden a shared leaderboard data directory", "srv/game/leaderboards", // dir
    "A game server writes per-player leaderboard files here from multiple worker processes running as different service accounts; no worker should be able to overwrite another's file.",
    "1777", ["Base: 777 for cross-account shared write access.", "Sticky bit protects each worker's files from the others.", "1 + 777 = 1777."],
    "Multi-tenant write access from different service accounts (not just different human users) is just as valid a reason to reach for sticky as the classic multi-user /tmp case."),
  c("e12", "Expert", 200, "SUID and SGID together on a diagnostics binary", "usr/local/bin/full-diag",
    "A diagnostics tool that needs to run as both a specific privileged user (for hardware access) and a specific privileged group (for log access) regardless of who invokes it.",
    "6755", ["SUID (4) for the owner-privilege requirement.", "SGID (2) for the group-privilege requirement.", "4 + 2 = 6, so 6 + 755 = 6755."],
    "Setting both SUID and SGID is rare but valid when a tool genuinely needs both a specific effective user and a specific effective group — each Linux security audit should treat 6xxx binaries as a top-priority review item."),
  c("e13", "Expert", 200, "Identify and fix a maximally over-permissioned binary", "usr/local/bin/everything-tool",
    "You discover a binary set to 7777 — SUID, SGID, sticky (meaningless here) and world read/write/execute all at once. This is about as bad as Linux permissions get. Fix it to a safe, standard shared executable.",
    "755", ["7777 combines every special bit and full world access — sticky does nothing on a file, and SUID+SGID+world-writable together is catastrophic.", "The safe fix removes all three special bits and world write.", "Target: plain 755."],
    "A 7777 file is effectively a standing invitation: anyone can overwrite the binary (world-writable) and it will then run with elevated owner and group privilege (SUID+SGID) for whoever runs it next."),
  c("e14", "Expert", 200, "Reproduce /var/log/wtmp permissions", "var/log/wtmp",
    "The login-history database that utilities like `last` read. It needs to be group-writable by the utmp group for logging daemons, and world-readable so any user can check login history.",
    "664", ["Owner and group: read + write.", "Others: read only.", "No special bits needed — this is a plain, if slightly unusual, triad case."],
    "wtmp is a good reminder that not every 'expert' case involves special bits — sometimes the expert-level skill is just correctly reasoning about an unusual but real-world triad."),
  c("e15", "Expert", 200, "Reproduce /etc/shadow permissions exactly", "etc/shadow",
    "Password hashes. Root needs full access; a dedicated 'shadow' group needs read access for tools like `passwd` to function via group membership rather than SUID alone on some distros; nobody else gets anything.",
    "640", ["Owner: read + write.", "Group (shadow): read only.", "Others: nothing, ever."],
    "Some distributions rely on group-based access (via the shadow group) as an alternative or supplement to SUID binaries needing to read this file — both approaches reduce how many programs need full root privilege just to check a password."),
  c("e16", "Expert", 200, "Reproduce /etc/gshadow permissions", "etc/gshadow",
    "Group password hashes and administrator lists — the group-oriented sibling of /etc/shadow, with identical sensitivity and identical reasoning for its mode.",
    "640", ["Same reasoning as /etc/shadow, applied to group credentials.", "Owner: read + write.", "Group: read only; others: nothing."],
    "gshadow is easy to forget about entirely, but it deserves the exact same scrutiny as shadow since it can also hold hashed credentials."),
  c("e17", "Expert", 200, "Lock down Kubernetes-style client certificates", "etc/kubernetes/pki/client.key",
    "A private TLS client key used to authenticate to a cluster control plane. Exactly as sensitive as an SSH private key, and should be treated identically.",
    "600", ["This is a private key, full stop — same rule as any SSH key.", "Owner: read + write only.", "Group and others: nothing."],
    "Modern infrastructure introduces new file types (certs, tokens, kubeconfigs) but the underlying permission rule for 'this is a private credential' never changes: 600."),
  c("e18", "Expert", 200, "Harden a multi-tenant upload folder across services", "srv/multi-tenant/uploads", // dir
    "Several independent services, each running as its own account within one shared group, write tenant uploads here; no service should be able to delete another's tenant data.",
    "1770", ["Base: 770 — group-wide access, others excluded entirely.", "Sticky bit protects each service's files from the others despite shared group write.", "1 + 770 = 1770."],
    "This closes the loop on the Expert tier's recurring theme: sticky bit protects shared-write directories regardless of whether the 'multiple writers' are human users or independent services."),
  c("e19", "Expert", 200, "Restrict a shared container runtime socket", "var/run/docker.sock",
    "Access to this socket is equivalent to root on the host, since it controls the container runtime. Only a tightly controlled group should be able to reach it — not world-accessible under any circumstance.",
    "660", ["Owner and group: read + write.", "Others: absolutely nothing — this is one of the most dangerous world-accessible files possible.", "No special bits apply to sockets."],
    "Docker socket exposure is a well-known real-world privilege-escalation path precisely because access to it is equivalent to root — treating its group membership as equivalent to root/sudo access is the correct mental model."),
  c("e20", "Expert", 200, "Capstone: audit a critical world-writable SUID root binary", "usr/local/bin/legacy-root-tool",
    "The single worst realistic misconfiguration this lab covers: a SUID-root binary that is also world-writable. Any local user could overwrite it with arbitrary code that then executes as root the next time anyone runs it. Fix it completely.",
    "755", ["This combines the Advanced-tier a20 scenario with everything you've learned about special bits.", "The fix removes the SUID bit entirely and removes world-write access.", "Target: plain 755 — a normal, safely shared executable with zero special privilege."],
    "This capstone deliberately repeats the lab's most important lesson one final time: SUID root plus world-writable is the single most dangerous permission combination in the entire Linux permission model, and spotting it should become automatic."),
  c("e21", "Expert", 200, "Reproduce a sudoers.d drop-in file", "etc/sudoers.d/app-deploy",
    "A per-application sudo override file inside /etc/sudoers.d/. Edits always go through visudo, which validates syntax before writing — nothing should be reading or writing this file outside that flow, including at runtime. Root needs read access to enforce the rules; nobody else needs anything.",
    "440", ["Owner and group: read-only, nothing more — not even root writes to it directly at runtime.", "Others: nothing whatsoever.", "This matches the real-world convention for files under /etc/sudoers.d/."],
    "440 is the actual convention sudo itself expects for files in this directory — read-only for owner and group, zero access for others, and no write bit for anyone since visudo is the only sanctioned way to change it."),
  c("e22", "Expert", 200, "Base mode for an about-to-be-immutable backup", "var/backups/resolv.conf.bak", // a backed-up config
    "A known-good backup of a critical DNS config, kept specifically so it can never be silently corrupted — you're about to chattr +i it as a final safeguard. Before that, its base mode should already reflect that it's a reference copy: owner and group read-only, others nothing.",
    "440", ["chattr +i is the last step, not a substitute for a correct base mode.", "Owner: read-only — even the owner shouldn't casually edit a reference backup.", "Group: read-only. Others: nothing."],
    "440 read-only-even-for-the-owner reflects that this file's whole purpose is to never change casually; chattr +i then makes that intent unbreakable at the kernel level, closing the loop with the earlier resolv.conf challenges in this lab."),
];
